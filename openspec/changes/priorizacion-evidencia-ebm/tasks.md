## 1. Tipos de publicación desde PubMed (sin consultas extra)

- [ ] 1.1 En `backend/external/pubmed.py`, `_esummary()` agrega `pubtypes` (lista de `doc["pubtype"]`, `[]` si falta) a cada entrada que devuelve `fetch_metadata()`, sin cambiar la cantidad de requests; verificar con un test en `tests/test_pubmed.py` que mockea `esummary` con y sin `pubtype` y afirma la lista devuelta
- [ ] 1.2 En `backend/pipeline/verification.py`, agregar `publication_types: list[str]` (default vacío) a `SourceVerification` y completarlo en `_verify_one()` solo cuando el veredicto es `verificada`; verificar con tests en `tests/test_verification.py` (verificada conserva los tipos, discordante/inexistente/no_verificable quedan con lista vacía) y que `pytest tests/test_verification.py` pase completo

## 2. Reglas de clasificación EBM (`backend/pipeline/evidence.py`)

- [ ] 2.1 Crear `backend/pipeline/evidence.py` con docstrings en español, `HypothesisStatus`, el modelo Pydantic `EvidenceAssessment`, las tablas de tipos de publicación como constantes y `source_ceiling()` según design D2–D5; verificar con `tests/test_evidence.py` cubriendo cada escenario del requisito "Tope de evidencia por tipo de publicación verificado" (meta-análisis, observacional, solo `Journal Article`, reporte de caso, mezcla, retractada, mayúsculas/espacios)
- [ ] 2.2 Implementar `classify_hypothesis()` (estado, tope, nivel efectivo, `verified_sources`, mejor fuente y `note` en español) leyendo solo el dict de veredictos; verificar en `tests/test_evidence.py` con hipótesis del caso base (neuropatía axonal sensitivomotora, varón de 42 años: déficit de B12 por metformina, CIDP, amiloidosis TTR) todos los escenarios de "Nivel de evidencia efectivo", "Estado bibliográfico" y "Trazabilidad del nivel asignado"
- [ ] 2.3 Agregar a `tests/test_evidence.py` los escenarios de "Solo cuentan los veredictos de la verificación": fuente con `verified: true` autodeclarado sin veredicto `verificada` no respalda, y tipos de publicación inventados en la `Source` no cambian el tope; verificar que pasan
- [ ] 2.4 Implementar `prioritize()` con la clave estado → nivel efectivo → prioridad → fuentes verificadas (desc) → orden original (orden estable); verificar en `tests/test_evidence.py` los escenarios de "Orden de las hipótesis en el reporte" y el de determinismo (dos llamadas, mismo resultado)

## 3. Contrato de exportación y armado del reporte

- [ ] 3.1 Agregar campos aditivos con default: `Source.publication_types` en `backend/models/hypothesis.py`; `RankedHypothesis.declared_evidence_level`, `RankedHypothesis.evidence_note` y comentario de `status` con `pendiente` en `backend/api/schemas.py`; `VerificationSummary.hipotesis_pendientes` y `hipotesis_topeadas`; verificar con un test que valida como `StructuredReport` un JSON sin ninguno de los campos nuevos y que `POST /api/report/pdf` lo acepta (escenario "Reporte previo sigue siendo válido")
- [ ] 3.2 En `backend/pipeline/report_builder.py`, reescribir `_rank_hypotheses()` como adaptador de `evidence.prioritize()` (conserva el cálculo de `supporting_agents`), exportando `evidence_level` efectivo, `declared_evidence_level`, `evidence_note`, `status` y `verified_sources` desde la evaluación; verificar con tests nuevos en `tests/test_report_builder.py` (nivel topeado, respaldada antes que especulativa, nivel antes que prioridad)
- [ ] 3.3 Ajustar `_annotate_source()` para limpiar `verified`, `verification_status`, `actual_title` y `publication_types` cuando la fuente no tiene veredicto, y completar `publication_types` solo si es `verificada`; ajustar `_build_verification_summary()` con `hipotesis_pendientes` y `hipotesis_topeadas` de modo que respaldadas + pendientes + especulativas = total; verificar con tests de conteo en `tests/test_report_builder.py`
- [ ] 3.4 Actualizar los tests existentes que dependían del comportamiento previo: `test_ordena_por_evidencia_dentro_de_misma_prioridad` (pasarle veredictos para que el nivel no se tope a III) y `test_sin_verificaciones_todo_queda_especulativo` (ahora `pendiente` para hipótesis con PMID); verificar que `pytest tests/` pasa completo

## 4. Presentación en frontend y PDF

- [ ] 4.1 Leer la guía local de Next.js indicada en `frontend/AGENTS.md`; en `frontend/src/lib/types.ts` agregar `"pendiente"` a `HypothesisStatus`, `declared_evidence_level?` y `evidence_note?` a `RankedHypothesis`, `publication_types?` a `Source` y `hipotesis_pendientes?`/`hipotesis_topeadas?` a `VerificationSummary`; verificar con `npm run lint` en `frontend/`
- [ ] 4.2 En `frontend/src/app/report/page.tsx`, agrupar la pestaña de hipótesis por estado con encabezados (respaldadas, pendientes de verificación, especulativas; omitir grupos vacíos), agregar badge para `pendiente`, mostrar "declarado N" junto al nivel cuando fue topeado y la `evidence_note`, y corregir el total del banner para incluir pendientes; verificar con `npm run build` y cargando en la vista el `reporte.json` que genera el demo (tarea 5.1)
- [ ] 4.3 En `backend/pipeline/pdf_exporter.py`, agrupar las hipótesis por estado con encabezados, agregar etiqueta y color para `PENDIENTE`, mostrar el nivel declarado cuando fue topeado, y sumar pendientes y topeadas a la portada y al aviso de verificación; verificar con tests en `tests/test_pdf_exporter.py` que extraen el texto del PDF y encuentran los encabezados de grupo, "PENDIENTE" y el nivel declarado

## 5. Evidencia para Trello (tarjeta #54)

- [ ] 5.1 Crear `scripts/demo_priorizacion_evidencia.py` (modo por defecto, sin red): hipótesis del caso base con veredictos fijos que cubran cada regla (meta-análisis que conserva I, observacional que topea I→II, reporte de caso en III, PMID alucinado → especulativa, PubMed caído → pendiente, sin fuentes, retractada); imprimir entrada (nivel declarado, fuentes, veredicto, tipos) → salida (tope, nivel efectivo, estado, nota, rank) y guardar en `output/demo_priorizacion_evidencia/` `priorizacion.txt`, `reporte.json` (StructuredReport) y `reporte.pdf`; verificar corriendo `python3 scripts/demo_priorizacion_evidencia.py` y comprobando los tres archivos
- [ ] 5.2 Agregar el modo `--pubmed` que verifica contra PubMed real un conjunto de PMIDs elegidos y confirmados durante la implementación (al menos un meta-análisis o revisión sistemática, un RCT, un observacional indexado solo como `Journal Article`, un reporte de caso y uno alucinado), dejando registrados en `priorizacion.txt` los tipos reales devueltos; verificar corriéndolo con red y revisando que los topes coinciden con la tabla de la spec

## 6. Documentación

- [ ] 6.1 Actualizar `.claude/architecture.md`: cerrar § "Pendiente — depende del Agente 04" (estado derivado con tres valores, sin "descartada", separación como lista única ordenada), corregir el comentario de orden de `StructuredReport.hypotheses`, documentar la tabla de topes y ajustar "Verificación final" del protocolo del debate; verificar releyendo que no queden menciones a "descartada" como estado vigente
- [ ] 6.2 Actualizar `.claude/CLAUDE.md` (tildar la priorización EBM en el Sprint 4, sumar `pipeline/evidence.py` al árbol y `demo_priorizacion_evidencia.py` a la lista de scripts) y `.claude/backlog.md` (tarea 11 → hecho con nota de las reglas y decisiones D3/D4/D10; hallazgo abierto sobre `Source(**s)` en `BaseAgent.parse_hypotheses()` aceptando campos de verificación del LLM); verificar con `openspec validate priorizacion-evidencia-ebm` y `pytest tests/`
